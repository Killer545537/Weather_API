Using *Serde* to read the JSON input.
Using *Colored* to beautify the terminal output.
Using *Regex* to parse the input (only for continuation of program).

Using OpenWeather API to get the weather information JSON.
